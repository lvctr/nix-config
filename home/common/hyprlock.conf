# Solarized-matching lock screen - background/text colours pulled from the
# same base03/base0 values as the rest of the theming.
background {
    color = rgba(002b36ff)
}

input-field {
    outer_color = rgba(268bd2ff)
    inner_color = rgba(073642ff)
    font_color = rgba(839496ff)
}
